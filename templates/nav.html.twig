<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Fast'Eat</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('index') }}"
          >Accueil <span class="sr-only">(current)</span></a
        >
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('fastfoods_list') }}">Tous les fast-food</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('fastfoods_list') }}">A propos</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('fastfoods_list') }}">Contact</a>
      </li>
    </ul>
    {% if not app.user %}
    <ul class="navbar-nav mr-auto">
      <li class="nav-item" style="float:right" >
          <a class="nav-link" href="{{ path('security_login') }}">Connexion</a>
      </li>
      <li class="nav-item" style="float:right" >
        <a class="nav-link" href="{{ path('security_registration') }}">Inscription</a>
      </li>
    </ul>
    {% else %}
    <ul class="navbar-nav mr-auto">
    <li class="nav-item">
      <a class="nav-link" href="{{ path('panel_admin') }}">Panel Admin</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="{{ path('panel_restaurateur') }}">Panel Restaurateur</a>
    </li>
    <li class="nav-item" style="float:right" >
      <a class="nav-link" href="{{ path('security_logout') }}">Déconnexion</a>
    </li>
    </ul>
    {% endif %}
  </div>
</nav>
